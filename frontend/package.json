{
  "name": "licenta-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bash -lc 'mkdir -p ../logs/frontend/vite && set -o pipefail && vite 2>&1 | tee -a ../logs/frontend/vite/dev.log'",
    "build": "bash -lc 'mkdir -p ../logs/frontend/vite && set -o pipefail && vite build 2>&1 | tee -a ../logs/frontend/vite/build.log'",
    "preview": "bash -lc 'mkdir -p ../logs/frontend/vite && set -o pipefail && vite preview 2>&1 | tee -a ../logs/frontend/vite/preview.log'"
  },
  "dependencies": {
    "lightweight-charts": "^4.2.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.10"
  }
}
